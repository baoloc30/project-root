

<h2>Quản lý sản phẩm</h2>
<a href="/products/new">+ Thêm sản phẩm</a>

<table>
  <thead>
    <tr>
      <th>Tên</th>
      <th>Giá</th>
      <th>Số lượng</th>
      <th>Nhà cung cấp</th>
      <th>Thao tác</th>
    </tr>
  </thead>
  <tbody>
    <% products.forEach(p => { %>
      <tr>
        <td><%= p.name %></td>
        <td><%= p.price %> đ</td>
        <td><%= p.quantity %></td>
        <td><%= p.supplier ? p.supplier.name : '' %></td>
        <td>
          <a href="/products/<%= p._id %>/edit">Sửa</a> | 
          <a href="/products/<%= p._id %>/delete" onclick="return confirm('Xóa sản phẩm này?')">Xóa</a>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>
